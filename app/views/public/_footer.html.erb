<footer class="account_footer">
    <br>
    <h4>Reach</h4>
    <br>
    <table id="footer_table">
      <tr>
        <td><%= link_to("Concierge", concierge_public_index_path) %></td>
        <td><%= link_to("About Us", about_public_index_path) %></td>
        <td><%= link_to("Contact Us", contact_public_index_path) %></td>

        <% if session[:username] %>

        <td><%= link_to("Sign Out", logout_public_index_path) %></td>

        <% else %>
        <td><%= link_to("Sign In", '#') %></td>
        <% end %>

      </tr>
    </table>
    <br>
</footer>